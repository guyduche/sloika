#!/bin/bash

BIN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

VARS="OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 THEANO_FLAGS=floatX=float32,device=cpu"

env $VARS ${BIN_DIR}/basecall_network.py $@
